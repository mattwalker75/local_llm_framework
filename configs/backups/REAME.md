Simple backups of previous configurstions.

To restore simply copy them to the parent "configs" directory and
rename them to "config.json" or "config_prompt.json" depending on 
the configuration file you are restoring.

